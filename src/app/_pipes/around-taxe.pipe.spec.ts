import { AroundTaxePipe } from './around-taxe.pipe';

describe('AroundTaxePipe', () => {
  it('create an instance', () => {
    const pipe = new AroundTaxePipe();
    expect(pipe).toBeTruthy();
  });
});
